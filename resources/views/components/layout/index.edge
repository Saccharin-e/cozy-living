<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title || "CozyKitchen" }}</title>
    
    @if ($slots.meta)
      {{{await $slots.meta()}}}
    @endif
     
    @vite(['resources/js/app.js'])
    
</head>
<body class="bg-gray-50">
    @include('partials/header-nav')
    
    {{{ await $slots.main() }}}
    
    @if ($slots.scripts)
      {{{await $slots.scripts()}}}
    @endif
</body>
</html>